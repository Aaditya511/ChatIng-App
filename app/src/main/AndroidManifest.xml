<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.chatzzandchat">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ChatzzAndChat">

        <activity android:name=".GetUserMobileNumActivity"></activity>
        <activity android:name=".all_activites.all_act.PersnolChatActivity" />
        <activity android:name=".all_activites.all_act.ChatActivity" />
        <activity android:name=".all_activites.all_act.SetProfileActivity" />
        <activity android:name=".UserSingUpActivity" />
        <activity android:name=".ui.activities.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>